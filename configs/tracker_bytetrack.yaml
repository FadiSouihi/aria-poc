tracker_type: bytetrack
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6
# 45 frames at 15 fps ≈ 3 s occlusion grace (Phase 1 exit criterion).
# At a different camera fps, scale this: track_buffer ≈ 3 × fps.
track_buffer: 45
match_thresh: 0.8
fuse_score: true